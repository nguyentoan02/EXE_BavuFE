name: Frontend Production Deployment (Traefik + Nginx)

on:
   push:
      branches: ["master"]

jobs:
   # --- GIAI ĐOẠN 1: BUILD & PUSH DOCKER ---
   build-and-push:
      runs-on: ubuntu-latest
      steps:
         - name: Checkout code
           uses: actions/checkout@v4

         - name: Set up Docker Buildx
           uses: docker/setup-buildx-action@v3

         - name: Login to Docker Hub
           uses: docker/login-action@v3
           with:
              username: ${{ secrets.DOCKER_USERNAME }}
              password: ${{ secrets.DOCKER_PASSWORD }}

         - name: Build and push
           uses: docker/build-push-action@v5
           with:
              context: .
              push: true
              # Đặt tên image (nhớ tạo secret DOCKER_IMAGE_NAME bên repo Frontend)
              tags: ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ github.run_number }}
              cache-from: type=gha
              cache-to: type=gha,mode=max

   # --- GIAI ĐOẠN 2: DEPLOY LÊN EC2 ---
   deploy:
      needs: build-and-push
      runs-on: ubuntu-latest

      # Khai báo môi trường để lấy Secret
      environment:
         name: production

      steps:
         - name: Deploy to EC2
           uses: appleboy/ssh-action@v1.0.3
           with:
              host: ${{ secrets.EC2_HOST }}
              username: ${{ secrets.EC2_USER }}
              key: ${{ secrets.EC2_SSH_KEY }}
              port: ${{ secrets.EC2_SSH_PORT || '22' }}
              script: |
                 # ========================================================
                 # 1. THIẾT LẬP BIẾN & LÀM SẠCH DỮ LIỆU
                 # ========================================================
                 VERSION="${{ github.run_number }}"
                 FULL_IMAGE="${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:$VERSION"

                 # Tên container cho Frontend (Khác với Backend)
                 CONTAINER_NAME="frontend-app" 

                 # Port của Nginx trong Dockerfile (Luôn là 80)
                 INTERNAL_PORT="80" 

                 # Lấy domain từ secret và xóa sạch ký tự lạ (khoảng trắng, tab, xuống dòng)
                 RAW_DOMAIN="${{ secrets.DOMAIN_NAME }}"
                 DOMAIN_NAME=$(echo "$RAW_DOMAIN" | tr -d '[:space:]')

                 echo "🚀 Bắt đầu Deploy Frontend Ver: $VERSION"
                 echo "🌐 Domain sạch: [$DOMAIN_NAME]"

                 # ========================================================
                 # 2. KÉO IMAGE & BACKUP
                 # ========================================================
                 docker pull $FULL_IMAGE

                 if [ "$(docker ps -q -f name=$CONTAINER_NAME)" ]; then
                     echo "⚠️ Đang backup container cũ..."
                     docker rename $CONTAINER_NAME "${CONTAINER_NAME}_backup"
                     docker stop "${CONTAINER_NAME}_backup"
                 fi

                 # Frontend thường không cần file .env lúc runtime (vì build xong là tĩnh)
                 # Nhưng nếu bạn có cần biến môi trường thì thêm đoạn tạo file .env ở đây

                 # ========================================================
                 # 3. CHẠY CONTAINER MỚI (TRAEFIK MODE)
                 # ========================================================
                 echo "🔥 Đang khởi chạy container Frontend..."

                 docker run -d \
                   --name $CONTAINER_NAME \
                   --network web_network \
                   --restart always \
                   -l "traefik.enable=true" \
                   -l "traefik.http.routers.$CONTAINER_NAME.rule=Host(\`$DOMAIN_NAME\`)" \
                   -l "traefik.http.routers.$CONTAINER_NAME.entrypoints=websecure" \
                   -l "traefik.http.routers.$CONTAINER_NAME.tls.certresolver=myresolver" \
                   -l "traefik.http.services.$CONTAINER_NAME.loadbalancer.server.port=$INTERNAL_PORT" \
                   $FULL_IMAGE

                 # ========================================================
                 # 4. HEALTH CHECK & ROLLBACK
                 # ========================================================
                 echo "fz Đang kiểm tra sức khỏe ứng dụng..."
                 sleep 10

                 if [ "$(docker ps -q -f name=$CONTAINER_NAME)" ]; then
                     echo "✅ DEPLOY FRONTEND THÀNH CÔNG!"
                     docker rm -f "${CONTAINER_NAME}_backup" || true
                     docker image prune -a -f --filter "until=24h"
                 else
                     echo "❌ THẤT BẠI! Container bị crash. Đang Rollback..."
                     docker rm -f $CONTAINER_NAME || true
                     
                     if [ "$(docker ps -a -q -f name=${CONTAINER_NAME}_backup)" ]; then
                         docker rename "${CONTAINER_NAME}_backup" $CONTAINER_NAME
                         docker start $CONTAINER_NAME
                         echo "✅ Đã khôi phục phiên bản cũ."
                     fi
                     exit 1 
                 fi

         # --- GỬI MAIL THÔNG BÁO (Tùy chọn) ---
         - name: Send Email Status
           if: always()
           uses: dawidd6/action-send-mail@v3
           with:
              server_address: smtp.gmail.com
              server_port: 587
              secure: false
              username: ${{ secrets.MAIL_USERNAME }}
              password: ${{ secrets.MAIL_PASSWORD }}
              subject: ${{ job.status == 'success' && '✅ FE DEPLOY SUCCESS' || '❌ FE DEPLOY FAILED' }}
              to: nvtvlog234@gmail.com
              from: GitHub Actions
              body: |
                 Dự án: Frontend ${{ github.repository }}
                 Phiên bản: ${{ github.run_number }}
                 Trạng thái: ${{ job.status }}
                 Domain: ${{ secrets.DOMAIN_NAME }}
